import React from 'react'
import styles from '../../../styles/components.module.scss'
interface Props {}

const BouncingBall = (props: Props): JSX.Element => {
  return <div className={styles.loader}></div>
}

export default BouncingBall
